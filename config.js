// config.js
// Your frontend doesn’t talk directly to OpenRouter anymore — the backend handles it
// So we don’t actually need the API key here for now
// Keep this file in case you want to store other frontend configs

const CONFIG = {
  botName: "TurboAssistant",
  placeholderText: "Type your message..."
};
